"use client";

// Gründungsangebot ended 2026-06-30 — banner disabled, page removed.
// Component kept as a stub because Header.tsx offsets its layout via
// useAnnouncementVisible(). Re-enable by restoring the pre-2026-07-09
// version from git history when a new announcement is needed.

export default function AnnouncementBanner() {
  return null;
}

export function useAnnouncementVisible() {
  return false;
}
